# Python-Fetch-Weather-Data-Project
Fetches the current Weather Data of a given location, without needing to convert the City name/Location into Longitudes and Latitudes through the 'OpenWeather' API
